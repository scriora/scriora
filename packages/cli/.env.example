# scriora-cli environment variables
# Reference: scriora-docs/architecture/SCRIORA_REPOSITORY_SPECIFICATIONS.md
#
# NOTE: User credentials (tokens) are stored in ~/.scriora/config.json
#       managed by the `conf` package — NEVER in env vars.

# Override the default API URL (optional)
SCRIORA_API_URL="http://localhost:4000"
